# Headers for /api responses. Flask already sets nosniff, frame and referrer headers itself.
# Nginx rule: a location with any add_header inherits none from its parents, so every location
# includes exactly the snippet it needs.
add_header Strict-Transport-Security $hsts_header always;
