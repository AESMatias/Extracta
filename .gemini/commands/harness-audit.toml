description = "Run the harness skill through a short, non-duplicating entry point."
prompt = "Delegate this request to the harness skill. Pass the invocation arguments unchanged: {{args}}\nDo not reproduce that skill's method here; load it and follow its own stopping and verification rules."
