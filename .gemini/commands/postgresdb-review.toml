description = "Delegate postgresdb review work to the installed postgres-reviewer agent."
prompt = "Delegate this request to the postgres-reviewer agent. Pass the invocation arguments unchanged: {{args}}\nDo not reproduce that agent's method here; load it and follow its own stopping and verification rules."
